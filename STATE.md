# STATE

No work in progress.

<!--
This file is how work survives a closed laptop. It describes NOW, not history.

RULES

1. Rewrite it in full. Never append. Nothing here is a record.
2. Update it after every completed step, not only at the end of a session.
   A crash should cost one step, not a session.
3. Keep it scannable in under two minutes. If it runs longer than a screen,
   the plan is too big and should be cut down.
4. When the work ships, empty this file back to "No work in progress" and let
   its content graduate: decisions to docs/decisions/, measured facts to
   MEMORY.md, the work itself into the commits.
5. It is committed, so it reaches the other machine and the other agent.

SHAPE, when work is in progress

# STATE

Updated: YYYY-MM-DD HH:MM
Branch:  <branch> @ <short sha>
Tests:   <n pass, n fail (which ones)>   or   not run

## Goal

One sentence. Why this work exists at all.

## Plan

- [x] a finished step
- [>] the step in progress        <- exactly one of these, ever
- [ ] a step not started

## Where I stopped

The specific thing, with file and line if it matters.

## Next action

The literal next command or edit. Not "continue the work".

## Blocked on

Decisions only the human can make. Empty means nothing is blocked.

## Do not

Approaches already tried that failed, so the next session does not redo them.
-->
